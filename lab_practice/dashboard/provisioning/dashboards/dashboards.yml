# Tells Grafana to auto-load any dashboard JSON in this folder.
apiVersion: 1

providers:
  - name: D7065E
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
